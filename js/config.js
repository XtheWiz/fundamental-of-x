// Site configuration. Replace placeholders with your real values.
//
// HOW TO SET UP BUY ME A COFFEE:
//   1. Sign up at https://www.buymeacoffee.com  (free, ~2 minutes)
//   2. Pick a username — that's your tip URL: buymeacoffee.com/USERNAME
//   3. Set `bmcUsername` below to that username and redeploy.
//
// Until bmcUsername is set, the tip buttons still render so you can see
// the design, but they show a setup hint on click instead of opening a
// (broken) link.

export const config = {
  bmcUsername: '', // e.g. 'xthewiz' — leave '' to keep buttons in "preview" mode
};
